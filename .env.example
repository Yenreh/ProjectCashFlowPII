# .env.example
# Copia este archivo a .env.local y configura tus valores reales
# NO commitear archivos .env.local con valores sensibles

# Base de datos PostgreSQL (opcional)
# Si no se configura, la aplicación usará datos de demostración en memoria
# DATABASE_URL="postgresql://user:password@host:5432/database"

# Ejemplo con Neon (recomendado para producción):
# DATABASE_URL="postgresql://user:pass@ep-name.region.aws.neon.tech/neondb?sslmode=require"

# Ejemplo con Supabase:
# DATABASE_URL="postgresql://postgres:password@db.project.supabase.co:5432/postgres"

# Ejemplo con PostgreSQL local:
# DATABASE_URL="postgresql://postgres:password@localhost:5432/finanzas"

# Google Gemini API Key (opcional - para IA, chat inteligente y escaneo de recibos)
# Obtén tu API key gratuita en: https://aistudio.google.com/app/apikey
# GEMINI_API_KEY="AIza..."

# ElevenLabs API Key (opcional - solo para síntesis de voz en el asistente)
# Obtén tu API key en: https://elevenlabs.io/
# ELEVEN_LABS_API_KEY="sk_..."

# NextAuth Configuration
# NEXTAUTH_URL solo es necesario en desarrollo local, Vercel lo detecta automáticamente
# NEXTAUTH_URL="http://localhost:3000"

# NextAuth Secret - genera uno con: openssl rand -base64 32
# NEXTAUTH_SECRET="tu_secret_aleatorio_aqui"

# Storage Mode para imágenes de recibos
# "local" = Guarda en /public/media/receipts (desarrollo local)
# "vercel_blob" = Usa Vercel Blob Storage (producción en Vercel)
# Default: "vercel_blob" si no se especifica
# STORAGE_MODE="local"

# Vercel Blob Storage (solo necesario si STORAGE_MODE=vercel_blob)
# En Vercel se configura automáticamente al habilitar Blob Storage
# Para desarrollo local con blob, obtén un token en: vercel.com/dashboard/stores
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_XXXXXXXXXX"
